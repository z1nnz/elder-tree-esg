# 共同開發規範

這個專題採「任何人都能貢獻、主要負責人決定是否進入正式版」的方式開發。`@z1nnz` 是主要負責人與全庫程式碼擁有者；`main` 代表可供其他成員拉取、建置與展示的正式版本。

## 開發流程

1. 先同步最新 `main`，再建立自己的功能分支；不要直接在 `main` 開發。
2. 分支名稱使用 `feature/主題`、`fix/主題`、`docs/主題`；Codex 建立的分支使用 `codex/主題`。
3. 一個合併請求只處理一個清楚交付目標，避免夾帶無關格式化、產物或私人資料。若同一份已確認的交付目標明確同時包含 App、研究文件與倉庫治理，可以放在同一個基礎版本，但合併請求必須分別列出驗證證據。
4. 推送分支後建立草稿合併請求，填寫變更內容、驗證證據與風險。
5. 先由 Codex 或另一位技術審查者檢查程式與測試，再由 `@z1nnz` 做最終產品核准。
6. 必要檢查全部通過、討論已處理且主要負責人核准後，才可合併。

## 不得直接提交

- `.env`、Firebase／雲端私鑰、憑證、權杖或密碼。
- 使用者個資、未去識別化照片、精確位置軌跡或正式合作方資料。
- Flutter、Next.js、Node、PlatformIO 或 Xcode 可重新建置的產物。
- 未經證實的實際植樹、碳權、健康效果、商家合作或政府合作宣稱。

## 送審前最低驗證

依變更範圍執行：

```sh
npm run typecheck
npm test

cd apps/mobile
flutter analyze
flutter test
```

涉及資料庫時，必須一併提交 Prisma migration；涉及使用者畫面時，確認名稱符合 [`CONTEXT.md`](CONTEXT.md)。硬體改動需附接線、供電、韌體版本與實機驗收證據。

## 審核責任

- 技術審查：確認資料一致性、權限、重複提交、失敗狀態、測試與相容性。
- 主要負責人審查：確認玩法、中文名稱、商業界線、展示內容及是否符合專題方向。
- GitHub 規則：`main` 禁止直接推送，必須經合併請求、程式碼擁有者核准與必要檢查。

Codex 不是獨立 GitHub 人類帳號，因此它的技術審查會以審查紀錄、合併請求留言或檢查報告留下；GitHub 上具有最終核准權的帳號仍是 `@z1nnz`。
