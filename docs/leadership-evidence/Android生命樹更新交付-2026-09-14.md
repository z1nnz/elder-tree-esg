# Android 生命樹更新交付

來源版本：`1a737e8c32be08e3ba8ca9f8d0d6f7fbfc3c4aee`。

## 成品與核對

本機安裝包：`apps/mobile/build/app/outputs/flutter-apk/app-debug.apk`，約 154 MiB，為除錯版、不是商店發布版。

APK SHA-256：`dcd155977e33808b3fe7c5c212ca101f9a1aa2e16b30a91b795dc77722cc20dd`。

執行 `tools/unity/prepare_life_tree_android.sh` 成功匯出最新生命樹庭園，再執行 `tools/unity/verify_life_tree_android_bridge.sh` 完成 Flutter 整包建置。檢查 APK 內 ARM64 `libunity.so`、`libil2cpp.so`，以及 ARMv7、x86-64 Flutter 執行時期均存在。

APK 內 `assets/bin/Data/level0` 與本次 Unity 匯出相同路徑 SHA-256 均為：`79be298ec5dd709a9197352a765c16e5a1f62abdea500e7f81259b1ac3be2897`。此比對證明封裝的是本輪匯出場景。

Android 匯出仍使用原本有後端成長狀態的「生命樹庭園」場景，不是強制成熟樹的獨立桌面外觀預覽。這輪更新包含現有模型、瀑布材質與手勢中斷修正，未擴充玩法或改變進度來源。

## 驗證與失敗處理

- Flutter `test/life_tree_garden_test.dart`、`test/life_tree_art_test.dart` 共 8 項通過；不以二維元件測試冒充 Unity 手機畫面驗收。
- 首次 IL2CPP 558 項更新成功（約 181 秒），最後 APK 封裝因磁碟不足失敗。
- 確認 Unity 匯出程序已退出後，刪除本工作樹 `apps/life-tree-unity/Library/Bee` 與 `Library/BurstCache` 共約 1 GiB 可再生編譯快取，保留 Android 成功編譯結果；這些快取可由 Unity 重建，不是從垃圾桶復原。
- 續跑封裝約 14.4 秒成功。沒有重新執行整個 Unity 匯出，沒有刪除原始碼、模型母稿或其他專題。
- 舊 Android 匯出保留於 `apps/life-tree-unity/Builds/Android_更新前保留`。
- 檢查後可用空間僅約 357 MiB，後續大型編譯仍須先處理容量；不宣稱空間問題永久解決。

## 未完成

`adb devices -l` 沒有可部署裝置。尚未驗證實機安裝、首次載入、返回 Flutter、再次進入、觸控、記憶體及每秒幀數。本輪未重新交付 iOS，未進行商店簽章或上架，也未宣稱完整可試辦產品已完成。二進位與建置暫存不加入 Git。
