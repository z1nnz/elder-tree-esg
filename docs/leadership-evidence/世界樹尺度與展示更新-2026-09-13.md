# 世界樹尺度與展示更新 — 2026-09-13

## 變更

延續使用者指定的世界樹單島外觀優先。前後葉冠在原分層枝梢造型上加厚，局部比例改為寬 1.10、深 1.08、高 1.60；前景中心降低 0.25、後景降低 0.10 模型單位。林地小樹尺寸縮小 20%，讓主樹與小樹有更大的尺度差距。

沒有改鏡頭、互動控制器、紀念掛點或新增玩法；不增加模型面数。仍為 99 網格、217,282 三角面、20,000 主樹葉片。整體美術與參考插畫仍有差距，不能以本次比例調整宣稱完成。

## 驗證與桌面交付

- Blender 六項河道檢查通過，保留河床共用三角拓撲修正。
- Unity 編輯器 33/33 通過，UTC 2026-09-12 16:45:47，結果存於 `screenshots/exterior-scale-2026-09-13/Unity測試結果.xml`。
- 使用 `TreeCompanion.Editor.CloudExteriorSceneBuilder.BuildMac` 成功重新建置 `apps/life-tree-unity/Builds/雲境外觀展示.app`，日誌 `/tmp/exterior-scale-native.log` 確認交付完成。
- 已退出舊程序並重新開啟新版；啟用視窗、按 R 後，原生視窗可見新的厚葉冠與較小林地。最初未啟用視窗時畫面停在暗色載入狀態，啟用後正常呈現；不把該初始畫面當作最終載入失敗。
- 新包 `Contents/Resources/Data/level0` SHA-256：`f78f1723d109cd072a3254dafa20125b33ead987e71c0c7b6a9cd765006a379e`。

## 界線與資源

桌面包已包含本日河流、枝梢與尺度修改，不再是前一日模型。此包仍是 Development Build 外觀展示，不代表 Flutter、Android、iOS 整合驗收；拖曳手動驗收尚未取得新證據。

建置前可用空間約 680 MiB、建置後约 462 MiB；沒有刪除專題資料。後續大型建置前必須重新檢查磁碟，不在低餘量下反覆打包。原外觀包由新建置更新，原始模型版本仍由 Git 保存。

每日研究檢查讀到五小時視窗剩餘 100%、每週剩餘 68%；本次優先交付使用者急需的外觀展示，未插入新研究或擴功能。
